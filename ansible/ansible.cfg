[defaults]
inventory           = inventories/dev/hosts.ini
remote_user         = ubuntu
private_key_file    = ~/.ssh/gitops-drift-shield-dev.pem
host_key_checking   = False
retry_files_enabled = False
stdout_callback     = ansible.builtin.default
result_format       = yaml
